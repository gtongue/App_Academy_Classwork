<h1>All bands</h1>
<ul>
    <% @bands.each do |band|%>
    <li> <%= link_to  "#{band.name}", band_url(band.id) %> </li>
    <% end %>
</ul>
<%= button_to  "Add new band", new_band_url, method: :get %>